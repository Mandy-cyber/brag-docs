# brag-docs

## Local install

- `git clone` this repo, then `cd brag-docs`
- `npm install`
- `npm run build`
- `npm link` (or `npm install -g .`) to get the `brag` command globally
- `brag init` in whatever folder you want your doc to live, then `brag add`, `brag build`, etc.